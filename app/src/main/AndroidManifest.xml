<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kanci">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <baseActivity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </baseActivity>
        <baseActivity android:name=".ui.login.LoginActivity" />
        <baseActivity android:name=".ui.test.TestActivity" />
        <baseActivity android:name=".ui.book.add.AddActivity" />
        <baseActivity android:name=".ui.book.plan.PlanActivity" />
        <baseActivity android:name=".ui.book.BookListActivity" />
        <baseActivity android:name=".ui.book.BookAddActivity" />
        <baseActivity android:name=".ui.task.TaskMainActivity" />
    </application>

</manifest>